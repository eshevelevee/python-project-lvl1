name: build-on-push
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build
        run:
          pip install poetry
          make install
      - name: lint-step
        run: make lint